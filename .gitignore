storage/raw/
storage/manifest.json
storage/parsed/
storage/oca.db-shm
storage/oca.db-wal
storage/oca.db.bak.*
vespers-*.html
snapshots/
!audit/snapshots/
!audit/snapshots/baseline.json
!test/snapshots/
audit/reports/

# Secrets — never commit
.env
.env.local

# macOS
.DS_Store

# Scraper intermediate output
*-scraped.json

# Working notes / prompts
agents/
booklet-print-calibrated.md
booklet-print-prompt.md
divine-liturgy-handoff.md
fix-booklet-blanks.md

# One-off test artifacts
2026-10-03
node_modules
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Per-user MCP config (contains absolute paths)
.mcp.json

# Translation overlay sandboxes
fixed-texts/translations/_*/

# Source PDFs (large; kept locally)
reference/*.pdf

# Photographed service books — local-only reference material
reference/**/*.HEIC
reference/**/*.heic
reference/**/*.jpg
reference/**/*.jpeg
reference/**/*.JPG
reference/**/Photos-*.zip

# Stray SQLite artifacts from misdirected DB opens (real DB lives at storage/oca.db)
audit/*.db
audit/*.db-shm
audit/*.db-wal

# Playwright e2e artifacts
test-results/
playwright-report/
